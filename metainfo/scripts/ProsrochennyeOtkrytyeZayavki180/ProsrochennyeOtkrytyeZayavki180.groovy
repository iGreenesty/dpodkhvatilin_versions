//Назначение:
/**
 * Параметров нет
 * Отправляем только текущую дату
 */
//ОСНОВНОЙ БЛОК--------------------------------------------------------

// Текущая дата - дата формирования отчета
table.addValue('rDate', new Date())